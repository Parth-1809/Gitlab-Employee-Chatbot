# Core framework
streamlit>=1.35.0

# LangChain ecosystem
# langchain: orchestration, chains, memory, prompts
# langchain-community: WebBaseLoader, FAISS wrapper
# langchain-google-genai: Gemini LLM + embeddings
langchain>=0.2.0
langchain-community>=0.2.0
langchain-google-genai>=1.0.5

# Vector store (local, no server needed)
faiss-cpu>=1.8.0

# Web scraping (used by WebBaseLoader internally)
beautifulsoup4>=4.12.0
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

# Gemini API client (pulled in by langchain-google-genai, pinned for stability)
google-generativeai>=0.5.0
